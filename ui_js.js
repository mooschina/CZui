function color(fc, bc, id){const element = document.getElementById(id);if (element){element.style.color=fc;element.style.backgroundColor=bc;}}
class TopImg extends HTMLElement{constructor(){super();const shadow = this.attachShadow({ mode: 'open' });const img = document.createElement('img');const src = this.getAttribute('src');if (src){img.src = src;}else{console.error('TopImg: src attribute is missing');}img.width = "auto";img.height = "50px";shadow.appendChild(img);}}customElements.define('top-img', TopImg);
class TopA extends HTMLElement{constructor(){super();const shadow = this.attachShadow({ mode: 'open' });const a = document.createElement('button');const href = this.getAttribute('href');a.onclick=()=>{window.location.assign(href || '#');};a.innerHTML = this.innerHTML;a.style.height = "50px";a.style.width = "auto";shadow.appendChild(a);}}customElements.define('top-a', TopA);
